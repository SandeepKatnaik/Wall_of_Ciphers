<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/container3"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <!--ImageView2 put lock picture to overall activity-->
    <ImageView
        android:id="@+id/bg2"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:src="@drawable/bg"
        android:scaleType="centerCrop" />

    <!--ImageView for key image-->
         <ImageView
              android:layout_width="wrap_content"
              android:layout_height="wrap_content"
              android:src="@drawable/key"
              android:layout_marginTop="50dp"
              android:layout_gravity="center_horizontal" />

      <!--Button 2: Move to EncryptActivity
        onclick :gotoEncryption
      -->
        <Button
            android:id="@+id/btn_ed"
            android:layout_gravity="center_horizontal"
            android:layout_width="200dp"
            android:layout_height="wrap_content"
            android:textFontWeight="1"
            android:backgroundTint="@color/white"
            android:layout_marginTop="350dp"
            android:gravity="center_horizontal"
            android:text="Encrypt and Decrypt"
            android:textAllCaps="false"
            android:textColor="@color/black"
            android:onClick="goToEncryption"
        ></Button>


    <!--Button 3: Move to EncryptActivity-->
    <Button
        android:id="@+id/btn_cipher"
        android:layout_gravity="center_horizontal"
        android:layout_width="200dp"
        android:layout_height="wrap_content"
        android:textFontWeight="1"
        android:backgroundTint="@color/white"
        android:layout_marginTop="450dp"
        android:gravity="center_horizontal"
        android:text="Cipher Details"
        android:textAllCaps="false"
        android:textColor="@color/black"
        android:onClick="open_cipher"
        ></Button>

    <!--ImageView for key image-->
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/key"
        android:layout_marginTop="600dp"
        android:layout_gravity="center_horizontal" />

</FrameLayout>

